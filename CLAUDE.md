# 里奥记账 — CLAUDE.md

---

## 项目信息

| 属性 | 值 |
|------|-----|
| 项目名称 | 里奥记账 |
| 项目类型 | 跨平台桌面记账应用 |
| 目标平台 | Windows 10/11 + macOS 12+ |
| 创建日期 | 2026-08-05 |
| 当前阶段 | 产品设计 & 技术选型 |

---

## 项目概述

里奥记账是一款轻量、易用的个人记账桌面应用。帮助用户记录每一笔人民币支出，通过两级分类体系实现精细化的消费管理。

- **核心定位**: 个人日常记账工具
- **目标用户**: 有个人财务管理需求的普通用户
- **设计理念**: 简单、直观、隐私优先（数据全部本地存储）

---

## 产品功能清单

### P0 — 核心功能（MVP）

| 模块 | 功能 | 说明 |
|------|------|------|
| 记账 | 添加支出 | 输入金额、选择分类、日期（默认当天）、可选备注 |
| 分类 | 两级分类 | 先选一级大类，再选二级小类（详见下方分类表） |
| 账本 | 历史记录 | 按日期倒序展示所有支出，支持按时间/分类筛选 |

### P1 — 重要功能

| 模块 | 功能 | 说明 |
|------|------|------|
| 统计 | 分类饼图 | 查看某一月份各分类支出占比 |
| 统计 | 月度趋势 | 柱状图展示每月总支出变化 |
| 账本 | 编辑/删除 | 修改或删除已有记录 |

### P2 — 增强功能

| 模块 | 功能 | 说明 |
|------|------|------|
| 预算 | 月度预算 | 设置月度支出上限，超支预警提示 |
| 导出 | 数据导出 | 导出为 CSV / Excel |
| 数据 | 备份恢复 | 本地数据库备份与恢复 |

---

## 数据字段设计

每笔支出记录包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 唯一标识 |
| amount | REAL | 金额（元），人民币，精确到分 |
| category_l1 | TEXT | 一级分类名称 |
| category_l2 | TEXT | 二级分类名称 |
| date | TEXT | 日期，格式 `YYYY-MM-DD` |
| note | TEXT | 备注，可选 |
| created_at | TEXT | 创建时间戳 ISO 8601 |

---

## 支出两级分类体系

```
🍜 餐饮美食
  ├── 三餐
  ├── 零食小吃
  ├── 饮品
  └── 聚餐宴请

🚗 交通出行
  ├── 公共交通
  ├── 出租车/网约车
  ├── 燃油/充电
  ├── 停车费
  └── 航空/铁路

🛒 购物消费
  ├── 服装鞋帽
  ├── 数码产品
  ├── 家居用品
  ├── 个护美妆
  └── 日用百货

🏠 住房生活
  ├── 房租/房贷
  ├── 水电燃气
  ├── 物业费
  ├── 网络通讯
  └── 维修保养

🎮 休闲娱乐
  ├── 影视音乐
  ├── 运动健身
  ├── 游戏充值
  ├── 旅游度假
  └── 宠物花费

🏥 医疗健康
  ├── 门诊医疗
  ├── 药品购买
  ├── 体检保健
  └── 牙科眼科

📚 教育学习
  ├── 培训课程
  ├── 书籍教材
  ├── 考试报名
  └── 文具用品

🎁 人情往来
  ├── 送礼红包
  ├── 孝敬父母
  ├── 婚礼份子
  └── 慈善捐款

💰 金融保险
  ├── 保险费
  ├── 贷款利息
  ├── 投资理财
  └── 手续费用

🔧 其他支出
  ├── 快递邮寄
  ├── 美容美发
  ├── 会员订阅
  └── 其他杂项
```

---

## 技术方案（待用户选定）

> 以下是 4 个候选方案，在用户选定方案之前，不进行任何代码初始化工作。

### 方案 A: Electron + React + TypeScript + better-sqlite3
- 体积：~150-200MB（含 Chromium）
- 最成熟、生态最丰富、开发效率最高
- 劣势：体积大、内存高

### 方案 B: Tauri v2 + React + TypeScript + Rust (rusqlite)
- 体积：~5-10MB
- 轻量高性能、启动快、安全性好
- 劣势：Rust 学习成本

### 方案 C: Flutter Desktop + Dart + drift
- 体积：~50-100MB
- 一套代码可扩展移动端
- 劣势：桌面端生态较新

### 方案 D: Wails v3 + React + TypeScript + Go
- 体积：~5-15MB
- Go 语言简单易学
- 劣势：社区规模较小

---

## 🚨 项目开发铁律（AI 必须遵守）

1. **技术决策权归用户**: 任何涉及技术栈、第三方库、架构模式、UI 框架、状态管理方案、打包方案、数据库选择等决策，AI **必须先列出 2-4 个可行方案**，逐一说明优劣势和使用场景，由用户做出最终选择。AI **严禁自行决定任何技术方案**。

2. **方案呈现格式**: 每个方案必须包含：方案名称、一句话简介、优势列表（至少 3 条）、劣势列表（至少 3 条）、适用场景说明。

3. **先文档后代码**: 所有功能需求、分类体系、数据模型等产品设计内容需先落地到本文档，再进行代码实现。

4. **数据本地存储**: 应用数据 100% 存储在用户本地设备，不连接任何远程服务器，不采集任何用户数据。

5. **人民币本位**: 金额统一为人民币（元），精确到小数点后两位。

6. **跨平台一致**: Windows 和 macOS 的功能、UI 保持完全一致。不得出现仅支持单平台的功能。

7. **文档持续更新**: 每次重要的技术决策确定后，必须更新本文档记录选型结果和理由。

---

## 技术决策记录

| 日期 | 决策项 | 选定方案 | 理由 |
|------|--------|----------|------|
| 2026-08-05 | 技术栈 | 方案 A — Electron + React + TypeScript + better-sqlite3 | 最成熟、生态最丰富、开发效率最高 |
| 2026-08-05 | 分类体系 | 10大类44小类（原设计） | 用户确认无需调整 |
| 2026-08-05 | 功能优先级 | P0→P1→P2 原计划 | 用户确认合理 |
| 2026-08-05 | 构建工具 | electron-vite | Vite 驱动，HMR 毫秒级，社区最活跃 |
| 2026-08-05 | UI 组件库 | Ant Design 5 | 中文支持好，表格/表单/日期选择器开箱即用 |
| 2026-08-05 | 状态管理 | Zustand | 轻量(<2KB)，API 简洁，适合中小应用 |
| 2026-08-05 | 图表库 | @ant-design/charts | 与 Ant Design 5 深度集成，基于 G2 |
| 2026-08-15 | 测试框架 | Vitest + @testing-library/react + jsdom + @vitest/coverage-v8 | 与 electron-vite 同源，复用 Vite 配置，组件测试与覆盖率开箱即用 |
| 2026-08-15 | 代码审查 | ESLint 9 + eslint-config-ali + inline header 规则 | 阿里巴巴前端规约官方配置，强制文件头作者名 Harriol（eslint-plugin-header 与 ESLint 9 不兼容，改用官方 inline 规则） |
| 2026-08-16 | 构建工具版本 | vite 升级至 7.x（package.json overrides 强制统一） | vitest 4 需 vite ≥6，plugin-react 4 不支持 vite 8，三方在 vite 7 对齐，修复测试收集阶段崩溃 |
| 2026-08-16 | 安全审查 | eslint-plugin-security + AI 人工审查 | 插件自动化检测危险模式（eval/不安全正则/child_process 等），AI 人工补查 SQL 注入、密钥泄露、配置明文等工具盲区 |
| 2026-08-16 | 测试执行模式 | Vitest 串行执行（fileParallelism: false） | vitest 4 在 Windows + Node 22 下并行执行多文件时 worker 崩溃，串行更稳定且本套件规模小、耗时更短 |

---

## 当前技术方案

- **运行时**: Electron (最新稳定版)
- **前端**: React 18+ TypeScript
- **数据库**: better-sqlite3（本地 SQLite）
- **构建工具**: electron-vite（Vite 7，通过 overrides 统一版本）
- **UI 库**: Ant Design 5
- **状态管理**: Zustand
- **打包**: electron-builder（electron-vite 内置）
- **测试**: Vitest + @testing-library/react（单元测试与组件测试，jsdom 环境）
- **代码审查**: ESLint 9 + eslint-config-ali（阿里巴巴前端规约）+ inline header 规则（文件头作者名）
- **安全审查**: eslint-plugin-security（自动化安全基线）+ AI 人工审查（SQL 注入/密钥/配置明文）

---

## 项目结构

```
里奥记账APP/
├── CLAUDE.md              ← 本文档
├── README.md
├── vitest.config.ts        ← 单元测试配置（Vitest）
├── tests/                  ← 单元测试（不纳入 git 版本控制）
│   ├── setup.ts
│   └── data/ store/ pages/ main/
├── src/                    ← 源代码
│   ├── main/
│   │   └── db/queries.ts   ← 主进程查询构建纯函数
│   ├── preload/            ← 预加载脚本
│   └── renderer/           ← 渲染进程（React）
├── assets/                 ← 静态资源
├── docs/                   ← 设计文档
└── scripts/                ← 构建脚本
```
